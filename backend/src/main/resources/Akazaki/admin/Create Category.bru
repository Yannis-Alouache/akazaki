meta {
  name: Create Category
  type: http
  seq: 2
}

post {
  url: {{url}}/api/admin/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "name": "Drinks"
  }
}